#!ipxe

# This doesn't really works. This ISO is not compatible with memdisk. :-(

iseq ${arch} x86_64 && set gp_arch amd64 || set gp_arch i486

set ver 0.16.2-11
set project gparted
set iso ${project}-live-stable/${ver}/${project}-live-${ver}-${gp_arch}.iso

imgverify sourceforge.ipxe ${sigs}sourceforge.ipxe.sig
chain sourceforge.ipxe
