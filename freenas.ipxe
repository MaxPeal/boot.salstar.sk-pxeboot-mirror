#!ipxe

set project freenas
set ver 8.3.0
iseq ${bits} 64 && set fnarch x64 || set fnarch x86
set iso FreeNAS-${ver}/RELEASE/${fnarch}/FreeNAS-${ver}-RELEASE-${fnarch}.iso
set sfboot memdisk
imgverify sourceforge.ipxe ${sigs}sourceforge.ipxe.sig
chain sourceforge.ipxe
